---
project:
  type: website
  output-dir: docs
title: "baseに関するメモ"
format:
  html:
    toc: true
    toc-title: 目次
    toc_float: true
    toc-depth: 2
    number-sections: true
    theme: sketchy
lang: ja
---

Visual Studio Codeを使うと便利なことが多い。
しかしながら，Visual Studio Codeに頼っていて，クリックの裏で何をしているのかを理解していない場合は，引数を変えてコマンドを実行したいときにどうすればよいかわからなかったりして困ることが想像できる。
このことを割り引いても，Visual Studio Codeを使うメリットは大きい。
なお，Visual Studio CodeはVS codeと表記されることもある。

インストールする。
パスを通す。

## base::apply()

```{r}
x <- 1:10
y <- x
```

以下のようにすると，`x`を同時に2つの変数に代入できる。

```{r}
y <- x <- 1:10
```

使う機会は一度もないかもしれないが，もしかしたら使う場面があるかもしれない。


## base::class()とbase::mode()とbase::typeof()

### data.frame

```{r}
str(iris)
attributes(iris)
```

